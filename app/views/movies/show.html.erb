<%= render @movie %>

<% @movie.cast_memberships.each do |cast| %>
  <%= cast.talent.name %> |
  <%= cast.character_name %>
<% end %>

<% @movie.cast_memberships.each do |cast| %>
  <%= cast.character_name %>
  <%= cast.talent.name %>
<% end %>
